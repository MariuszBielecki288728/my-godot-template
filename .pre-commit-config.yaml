repos:
  - repo: local
    hooks:
      - id: godot-dev-precommit
        name: Godot development checks
        entry: uv run --project tools --locked godot-dev precommit
        language: system
        pass_filenames: false
